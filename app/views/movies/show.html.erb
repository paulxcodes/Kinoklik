<h1> <%= @movie.title %> </h1>

<%= link_to "Edit this movie", edit_movie_path(@movie) %>

<%= link_to "Delete movie", movie_path(@movie), method: :delete, data: { confirm: "Are you sure you want to delete #{@movie.title}?" } %>
